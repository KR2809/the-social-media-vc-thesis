# Anthropic — required for LLM signal scoring (Haiku default, Sonnet for taxonomy)
ANTHROPIC_API_KEY=

# YouTube Data API v3 — required for video-creator outcome data
YOUTUBE_API_KEY=

# Reddit — required for community signals
REDDIT_CLIENT_ID=
REDDIT_CLIENT_SECRET=
REDDIT_USER_AGENT=bba-thesis-signal-fund/0.1 by /u/<your_reddit_handle>

# Product Hunt — required for launch data
PRODUCTHUNT_DEV_TOKEN=

# OpenAI — optional fallback for embeddings
OPENAI_API_KEY=

# Crunchbase — if academic free tier is approved (2-week wait); leave empty otherwise
CRUNCHBASE_API_KEY=

# Hard monthly cap reminders (informational only — set the actual caps in each provider's console)
# ANTHROPIC_MONTHLY_BUDGET_USD=30

# ---------------------------------------------------------------------------
# Supabase — DECISION_LOG iter-13 hybrid storage (Option C)
# Project: thesis-social-signal-fund (eu-west-2, free tier)
# ---------------------------------------------------------------------------
# Project URL — public, safe to commit to .env.example.
SUPABASE_URL=https://uhhcylfvoxgyrqijlxjk.supabase.co

# Anon key — read-only, public-by-design (RLS policies allow SELECT to anon).
# Safe to commit; this is the same key that ships in the thesis appendix so
# examiners can query the data themselves.
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InVoaGN5bGZ2b3hneXJxaWpseGprIiwicm9sZSI6ImFub24iLCJpYXQiOjE3Nzg3NjAxOTksImV4cCI6MjA5NDMzNjE5OX0.KqkjzY4ZDved5yaoINqTCCa3xUi6BkR_2FpQb6C1U9c

# Service-role key — bypasses RLS. NEVER commit. NEVER share publicly.
# Used ONLY by scripts/sync_to_supabase.py to write rows.
# Grab from: https://supabase.com/dashboard/project/uhhcylfvoxgyrqijlxjk/settings/api
SUPABASE_SERVICE_ROLE_KEY=

# GitHub Personal Access Token for scripts/publish_data_snapshot.py
# Needs `repo` scope to create releases. Generate at github.com/settings/tokens
GITHUB_TOKEN=
